package com.anygine.core.common.client.serialize;

import org.junit.Test;

public class QueryBuilderTest {

  @Test
  public void testEqualTo() {
/*
    com.anygine.core.common.client.metamodel.Player player = MetaModel.getPlayer();
    Query<Player> playerQuery = QueryBuilder.from(Player.class).
    where(player.profile.email).equalTo("foo").query();
    */
  }
}
